# 大模型成本优化

> 来源：飞书 Wiki《电商大模型成本优化三大案例拆解：如何年省千万元》
> 读取时间：2026-07-01
> 文档版本：revision 2170
> 本文定位：从原文中提炼出的“实战手册”，方便快速理解和落地，不是逐字摘抄。

## 0. 一句话结论

大模型成本优化的核心，不是单纯找更便宜的模型，而是建立一套让大模型只处理高价值问题的系统工程：先减少无效流量，再减少重复计算，再把固定任务小模型化或规则化，最后用 ROI 决定资源分配和生命周期退出。

可以把治理顺序理解成：

```text
先别进 LLM -> 必须进时少算 -> 可以异步时离线算 -> 固定任务专用模型算 -> 低 ROI 任务退出
```

## 1. 先统一尺子：三层单位成本

只看总成本、总 Token 或模型单价，很难判断“贵在哪里”。原文建议先建立三层单位成本口径。

| 层次 | 指标 | 计算方式 | 回答的问题 |
| --- | --- | --- | --- |
| L1 资源成本 | 单位 Token 成本 | 总成本 / 总 Token 消耗 | 模型选型、输入输出结构是否合理 |
| L2 调用效率 | 单次有效调用成本 | 总成本 / 有效业务调用量 | 有多少 Token 浪费在无效调用上 |
| L3 业务 ROI | 单位成本带来的业务增量产出 | 增量收入或业务价值 / 成本 | 每花一块钱产生多少业务价值 |

进一步升级后，可以形成四步决策链：

| 层次 | 关注点 | 决策问题 |
| --- | --- | --- |
| L1 量价分离 | 拆分量结构和价结构，如文本/图片/视频、输入/输出/缓存、内部/外部模型 | 是量的问题，还是价的问题 |
| L2 效率归因 | 定位浪费发生在入口、Prompt、缓存还是模型选型 | 浪费发生在哪个环节 |
| L3 ROI 分型 | 不同业务用不同 ROI 口径评估产出 | 谁是高价值必保，谁是低价值可砍 |
| L4 分配决策 | 在资源受限时做跨项目优先级排序 | 保谁、砍谁、谁排队 |

## 2. 通用治理地图

| 治理层 | 核心做法 | 直接影响 |
| --- | --- | --- |
| 入口层 | 价值识别、分层控量、ML 前置、规则粗筛 | 降低无效调用，改善 L2 |
| Prompt 层 | 变量后置、上下文压缩、Prompt lint、图片数量控制 | 降低 Token 消耗，改善 L1 |
| 执行层 | 前缀缓存、结果缓存、异步排队、离线批处理 | 减少重复计算并削峰，改善 L1 和 L2 |
| 模型和资源层 | 蒸馏小模型、专用模型、资源池、潮汐调度 | 提高资源利用率，改善 L1 |
| 治理层 | ROI 体系、准入控制、低 ROI 退出 | 让成本与业务价值对齐，改善 L3 |

## 3. 三个案例的精华

### 3.1 电商 B 侧数据策略：四层成本治理

这个案例的核心是从“全量调用大模型”转向“只让高价值样本进入大模型”。

| 治理层 | 做法 | 原文中的效果 |
| --- | --- | --- |
| Routing 请求路由 | 用 ML、Graph、MOE、XGBoost 先做召回、聚类、风险提纯 | 低价 SKU 场景在风险召回率 91.5% 前提下，大模型算力成本降低 62.34%；高风险商家风险浓度提升 100 倍 |
| Distillation 能力蒸馏 | 用闭源大模型构造样本，再用 SFT、GRPO 训练开源小模型 | SFT 压缩 COT 后推理速度提升 8 倍；同准召水平下 LLM 计算成本节约 77% |
| Inference Optimization 推理优化 | 前缀缓存、Batch Job、闲时算力、共享上下文复用 | Batch Job 价格约为在线推理 50%；结合前缀缓存后 LLM 计算成本节约 80% |
| Specialized Models 专用模型化 | 固定节点用小模型、函数式质检或规则替代通用大模型 | 自部署小模型替代后推理成本降低 80%；AI 选品质检大模型成本降低约 50%-60% |

几个可复用判断：

- 如果任务是“海量对象中找少量高风险对象”，先做 ML/规则/图算法粗筛，不要直接全量上 LLM。
- 如果任务样本可构造、目标可评估，就适合做蒸馏，小模型可以在窄任务上追平甚至超过大模型。
- 如果任务实时性不强，优先考虑离线批处理。原文中劣质画风案例用前缀缓存 + 离线批量推理，在准确率 93.8%、召回率 97% 不受影响下，成本从约 30 元/万件降到约 15 元/万件。
- 如果 Agent 或 Workflow 中存在边界清晰、规则性强的节点，应考虑小模型或函数式替代。

### 3.2 抖店 AI：模型系统与资源调度

这个案例的核心不是单项调用最便宜，而是建立模型策略、资源调度、效率优化和观测反馈的系统。

| 层级 | 系统要回答的问题 | 输出决策 |
| --- | --- | --- |
| 模型策略层 | 这个请求应该用哪个模型，是否进入灰度或实验 | 候选模型列表和调用顺序 |
| 资源调度层 | 该用保障资源还是非保障资源，当前水位是否允许借用 | 资源池、账号、模型实体和限流值 |
| 效率优化层 | 是否可复用上下文，是否应该排队而非同步占用 | 缓存命中、异步排队、同步执行 |
| 观测反馈层 | 调用是否稳定、是否省钱、是否该调参 | 阈值调整、模型替换、缓存治理、容量申请 |

关键做法：

- 低 ROI 用户或场景调用低成本模型，高 ROI 用户或关键场景调用 SOTA 模型。
- 保障资源用于在线核心链路，非保障资源用于普通、离线、可重试、可排队任务。
- 离线任务只在保障资源水位低于阈值时借用保障资源。原文给出的默认参考是平峰 70%，高峰 50%，低峰 90%。
- AIGC 长任务不适合全部同步调用，应使用异步优先级排队，避免用户请求超时、长任务占满模型资源、峰值打爆下游。
- 模型池通过多个模型实体、账号、资源类型和供应商形成兜底空间，应对限流、失败和波动。

前缀缓存是这个案例的重点收益项：方舟整体缓存命中率 65%+，节省成本 650 万/年。

### 3.3 智能审核：全链路控本增效

这个案例的核心是全链路治理：流量漏斗、Prompt 规范、缓存、资源池隔离、潮汐调度和 ROI 闭环。

| 环节 | 做法 | 原文中的效果 |
| --- | --- | --- |
| 流量漏斗 | 小模型粗召 + 中等模型提准 + 大模型审核 | 中等模型通常可再过滤 50% 流量；Qwen3.5-2B 微调模型成本约为豆包 1.6 的 1/5 |
| Prompt 规范 | 变量后置、禁止重复变量、控制图片数量、特征针对性选择 | 变量重复引用治理减少 511 亿 Token/天，占相关模型消耗总量 22.73% |
| 前缀缓存 | 显式前缀缓存 + 结果缓存 | 实际命中 8%，节省 3 万元/天，目标提升到 15%-20% |
| 资源池隔离 | 在线/离线、业务场景、重保/普通模型隔离 | 重保模型成功率 99.5%+，普通模型约 70% |
| 池化和潮汐调度 | 整合零散资源，夜间低谷处理离线回扫 | 池化调度增量供给 9600 万 TPM；潮汐调度让回扫吞吐提升 100% |
| ROI 生命周期 | 低 ROI 模型治理或退出 | 优化/下线模型 422 个，释放 500 亿+ Token/天，约 2 万元/天 |

这个案例说明：审核、风控、治理类业务不能只看单次模型成本，还要看单位有效拦截成本、漏放风险、重保成功率、低 ROI 模型占用等指标。

## 4. 前缀缓存的实操规则

前缀缓存是三个案例都反复验证的高 ROI 手段，但它不是简单打开开关就能生效。

### 4.1 System Prompt 必须“零变量”

前缀缓存基于精确 Token 序列匹配。System Prompt 中只要有一个动态变量、空格或标点不同，就可能导致缓存失效。

不要在 System Prompt 中放：

- 当前时间
- 用户 ID
- session_id
- 本轮请求动态参数
- 每次都可能变化的业务上下文

这些动态数据应放到 User Message 中。

### 4.2 尽量使用 system + user 的消息结构

原文提到，如果消息拼接成 system + user + assistant + user + assistant 的多轮形式，assistant 类型消息无法进行前缀缓存，可能降级为普通调用。要使用前缀缓存，应尽量让可复用内容稳定地放在 system prompt 或稳定 user 前缀中。

### 4.3 缓存有触发门槛

很多模型厂商对前缀缓存有长度要求。原文举例：OpenAI 和 Anthropic 通常要求相同静态前缀至少达到 1024 Tokens 才会开启缓存。

因此，过短的 System Prompt 即使完全固定，也可能无法触发缓存。可以把通用规则、输出格式、Few-shot 示例沉淀到 System Prompt 中，形成“胖 System”，提高缓存 ROI。

### 4.4 动态数据要结构化

动态内容进入 User Message 后，必须用清晰分隔符避免模型混淆指令和数据。例如：

```xml
<context>
此处注入动态上下文数据
</context>

<user_query>
用户真正的问题
</user_query>
```

System Prompt 里要提前声明如何读取这些标签，例如：“你需要根据 `<context>` 标签内的信息回答 `<user_query>` 提出的问题。”

### 4.5 缓存要运营化观测

建议把这些指标纳入日常监控：

- cached_tokens
- 缓存命中率
- prefixCacheHit
- prefixCacheRatio
- cache_creation_input_tokens
- cache_read_input_tokens
- 首 Token 延迟
- TPOT
- 输入 Token 成本下降比例

## 5. 落地优先级：看到什么问题，就先做什么

| 现象 | 优先动作 | 典型手段 |
| --- | --- | --- |
| 总成本高，但不知道贵在哪里 | 先建单位成本口径 | L1/L2/L3 基线、按场景拆账 |
| 大量请求没有明确业务价值 | 先做入口层控量 | ML 前置、规则粗筛、风险分层、低价值流量拦截 |
| Token 消耗异常高 | 先查 Prompt | 变量后置、去重复变量、压缩上下文、控制图片数量 |
| Prompt 固定但成本仍高 | 先做缓存 | 隐式/显式前缀缓存、结果缓存、session 粘性 |
| 在线任务挤占资源 | 先做异步和离线 | Batch Job、优先级队列、T+1 任务 |
| 某类任务稳定且标签明确 | 做蒸馏或小模型替代 | SFT、GRPO、教师模型样本生成、轻量分类模型 |
| 固定规则节点还在调用大模型 | 做专用模型化或函数化 | 函数式质检、规则引擎、固定 DAG |
| 高价值链路被低价值任务挤压 | 做资源隔离和 ROI 治理 | 重保资源、非保障资源、低 ROI 退出、资源水位阈值 |

## 6. 一套可执行的检查清单

1. 建立每个业务的 L1/L2/L3 单位成本基线。
2. 按业务场景拆出在线、离线、重保、评测、回扫、AIGC 长任务。
3. 找出调用量最大的 Top 场景，检查是否存在无效调用或低价值流量。
4. 对每个 Top 场景检查是否能用 ML、规则、图算法先做粗筛。
5. 对每个 Prompt 做 lint：变量是否后置，是否重复引用变量，图片和上下文是否过量。
6. 检查 System Prompt 是否绝对固定，是否有动态时间、用户 ID、session_id 等变量。
7. 建立缓存观测面板，持续看 cached_tokens、prefixCacheHit、命中率和首 Token 延迟。
8. 将可延迟任务迁移到离线批处理或异步优先级队列。
9. 对样本可构造、目标可评估的任务评估 SFT/GRPO 蒸馏。
10. 对所有模型建立 ROI 口径，低 ROI 模型进入治理、降级或退出流程。

## 7. 关键风险和注意事项

- 不要只追求更低模型单价。低价模型如果导致召回、准确率或业务转化下降，L3 ROI 可能更差。
- 不要把所有任务都做成同步链路。视频生成、图片生成、复杂素材生成等长任务应该异步化。
- 不要假设开启缓存就会命中。前缀必须稳定、长度足够，并且底层调度最好有账号或实例粘性。
- 不要让离线任务无条件借用保障资源。保障资源要按水位和时段动态借用，避免挤压在线链路。
- 不要让低 ROI 模型长期占用资源。成本治理必须有准入、观测、治理和退出闭环。

## 8. 最值得带走的判断框架

大模型成本治理可以用五个问题快速判断优先级：

1. 这个请求必须进 LLM 吗？
2. 如果必须进，能不能减少输入、输出或重复上下文？
3. 如果需要重复算，能不能缓存或复用？
4. 如果任务固定，能不能蒸馏成小模型、函数或规则？
5. 如果资源不够，谁的 ROI 更高，谁应该被保障、降级或退出？

把这五个问题回答清楚，成本优化就会从“砍预算”变成“按业务价值分配智能算力”。
